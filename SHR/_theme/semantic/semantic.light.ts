import { colorsLight } from '../tokens/colors.light';

export const semanticLight = {
  primary: colorsLight.primary,
  primaryDark: colorsLight.primaryDark,
  onPrimary: '#FFFFFF',

  success: colorsLight.success,
  warning: colorsLight.warning,
  danger: colorsLight.danger,
  error: colorsLight.danger,
  onError: '#FFFFFF',

  background: colorsLight.background,
  text: colorsLight.text,

  surface: colorsLight.surface,
  surfaceDark: colorsLight.surfaceDark,

  overlay: colorsLight.overlay,
};
